<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Members</title>
</head>
<body>
  <h1>Welcome, <%= user ? user.username : 'Guest' %>!</h1>
  <p>This is the members view.</p>
  
  <a href="/new-message">Create a new message</a>
  <a href="/log-out">Log Out</a>

  <h2>Messages:</h2>
  <% if (user.username === adminUsername) { %>
    <% if (messages.length > 0) { %>
      <ul>
        <% messages.forEach(function(message) { %>
          <li>
            <h3><%= message.username %> - <%= message.title %> - ID : <%= message.id %></h3>
            <p><%= message.message %></p>
            <small>Posted on <%= new Date(message.time).toLocaleString() %></small>
            <form action="/delete/<%= message.id %>" method="POST" style="display:inline;">
              <button type="submit">Delete</button>
            </form>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <p>No messages yet.</p>
    <% } %> 
  <% } else { %>
    <% if (messages.length > 0) { %>
      <ul>
        <% messages.forEach(function(message) { %>
          <li>
            <h3><%= message.username %> - <%= message.title %></h3>
            <p><%= message.message %></p>
            <small>Posted on <%= new Date(message.time).toLocaleString() %></small>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <p>No messages yet.</p>
    <% } %> 
  <% } %>
</body>
</html>
